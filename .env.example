# API server (backend)
PORT=6015
CLIENT_URL=http://localhost:5015
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27018/contentweave
JWT_SECRET=change-me-to-a-long-random-string
UPLOAD_DIR=./server/uploads

# React admin (client) — leave empty in dev to use Vite proxy; set for production builds
VITE_API_URL=
